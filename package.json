{
  "name": "p5_lighthouse",
  "version": "1.0.0",
  "description": "",
  "main": "parser.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "preact build --no-prerender; node index.js",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "chrome-launcher": "^0.15.1",
    "express": "^4.18.2",
    "lighthouse": "^9.6.8"
  }
}
